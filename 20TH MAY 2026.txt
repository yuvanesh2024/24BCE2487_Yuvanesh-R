/*constraints are regulations of the columns;
check-(canada,india)
insert into emp values('yuvanesh',20);*/

create database vitcampus;
use vitcampus;
CREATE TABLE Persons (
    ID int NOT NULL,
    LastName varchar(255) unique not null,
    FirstName varchar(255) NOT NULL,
    Age int
);
desc persons;
insert into persons values(105,'yuvanesh','R',20);
select * from persons;

ALTER TABLE Persons
ADD UNIQUE (AGE);

ALTER TABLE Persons
DROP CONSTRAINT Age;
insert into persons values(102,'yuvan','E',NULL);
ALTER TABLE Persons
ADD UNIQUE (AGE);
select * from persons;

DROP TABLE PERSONS;
DROP TABLE cricket;
DROP TABLE cricket;
CREATE TABLE CRICKET(
    ID int NOT NULL,
    LastName varchar(255) NOT NULL,
    FirstName varchar(255),
    Age int,
    CHECK (Age>=18)
);
insert into cricket values(107,'suresh','raina','17');
/*Error Code: 3819. Check constraint 'cricket_chk_1' is violated.*/
insert into cricket values(101,'virat','chokli','20');
select * from cricket;
CREATE TABLE MATHS(
    ID int NOT NULL,
    LastName varchar(255) NOT NULL,
    FirstName varchar(255),
    Age int
);
insert into maths values(102,'virat','kohli','99');
select * from maths;
ALTER TABLE MATHS
ADD CHECK(AGE<=21);
/*Error Code: 3819. Check constraint 'maths_chk_2' is violated.*/

insert into maths values(102,'virat','kohli','99');
select * fro
m maths;
CREATE TABLE MOUSE(
    ID int NOT NULL,
    LastName varchar(255) NOT NULL,
    FirstName varchar(255),
    CITY VARCHAR(150) defauT 'VELLORE';
);
CREATE TABLE PRODUCTS(
PNAME VARCHAR(160) NOT NULL,
PREFNO  INT NOT NULL UNIQUE
);
DESC PRODUCTS;
INSERT INTO PRODUCTS  VALUES("MONKEY",19800000);
SELECT * FROM PRODUCTS;

ALTER TABLE PRODUCTS
ADD PRIMARY KEY(PREFNO);

ALTER TABLE PRODUCTS
DROP PRIMARY KEY;

ALTER TABLE Persons
DROP PRIMARY KEY;
drop table products;

create table category(
cid int primary key,
cname varchar(100) not null
);

insert into category values (101,'electronics'),(102,'Home App');

select * from category;
drop table products;
CREATE TABLE products (
    pid int primary key,
    pname varchar(100) NOT NULL,
    cid int, 
    FOREIGN KEY (cid) REFERENCES category(cid)
);
desc products;

delete from category where cid=101;
update 

insert into products values (504,'Power Bank Samsung',64-bit encry key);

select * from products;